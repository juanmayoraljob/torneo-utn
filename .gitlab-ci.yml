image: node:20

cache:
  paths:
    - node_modules/

stages:
  - deploy

pages:
  stage: deploy
  script:
    - npm ci
    - export VITE_BASE_PATH="/${CI_PROJECT_NAME}/"
    - npm run build
    - mv dist public
  artifacts:
    paths:
      - public
  only:
    - main
    - master
